#include <stdio.h>

unsigned int T;
short sx, sy, ax, ay, n, i;
short x[50], y[50] r[50];
 
int main()
{
	scanf("%u",&T);
	
	for(; T; --T)
	{
		scanf("%hd %hd %hd %hd",&sx, &sy, &ax, &ay);
		
		scanf("%hd",&n);
		for(i = 0; i < n; ++i)
			scanf("%hd %hd %hd",&x[i], &y[i], &r[i]);
			
		// 반지름으로 거리 체크 
		
		
	}
	return 0;
}
